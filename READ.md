# Screen ทั้ง mid-cap และ large-cap (default)
python screener.py

# Screen เฉพาะ mid-cap
python screener.py --type midcap

# Screen เฉพาะ large-cap
python screener.py --type largecap

# Screen ทั้งสอง (เหมือน default)
python screener.py --type both